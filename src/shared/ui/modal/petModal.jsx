import React from 'react';
import cl from './petModal.module.scss'

const PetModal = ({children, visible, setVisible}) => {

    const rootClasses = [cl]

    return (
        <div>

        </div>
    );
};

export default PetModal;